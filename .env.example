# Copy to .env on the server:
#   cp .env.example .env
#
# The domain whose A record points at the VM's public IP.
RELAY_DOMAIN=peek.maos.dev
# Email for Let's Encrypt certificate expiry notices.
ACME_EMAIL=lampoon2gn@gmail.com
